#include <stdio.h>
#include <limits.h>
int main(){

printf("The max value of int = %d \n", INT_MAX);
printf("The minimum value of int = %d \n", INT_MIN);

printf("The max value of Long = %lld \n",LONG_LONG_MAX);
printf("The minimum value of Long = %lld \n", LONG_LONG_MIN);
    return 0;
}